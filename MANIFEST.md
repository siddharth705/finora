# Complete file bundle — everything changed across this entire session

Generated by diffing the current working state against the exact original zip you uploaded, so
this is a verified, complete list — not reconstructed from memory. 63 files: 15 new, 48 modified.
Unzip directly over your repo root; every path below is repo-relative.

Grouped by what each batch of work was for:

## PDF/CSV import bug fixes
- `backend/.../imports/TransactionNormalizer.java` — remarks/particulars description columns,
  DR/CR type detection, balance-column fallback (restored after an earlier regression)
- `backend/.../imports/StatementValidator.java` — balance-chain reconstruction (real bug found
  on your actual PNB statement)
- `backend/.../imports/BalanceChainUtil.java` **(new)** — shared algorithm both CSV/PDF paths use
- `backend/.../imports/pdf/PdfPreviewGenerator.java` — same balance-chain fix, PDF side
- `backend/.../test/imports/{BalanceChainUtilTest,StatementValidatorTest,TransactionNormalizerTest}.java` **(new)**

## Pagination (Ledger + admin Users directory)
- `backend/.../dto/PagedResponse.java` **(new)** — extracted from AdminDtos, now shared
- `backend/.../dto/AdminDtos.java`, `.../service/AdminUserService.java`,
  `.../controller/{AdminController,AdminUserController,AdminTransactionController}.java`
- `backend/.../transactions/{TransactionService,TransactionController}.java` +
  `test/.../TransactionServiceTest.java`
- `frontend/src/pages/Ledger.tsx`, `frontend/src/pages/Dashboard.tsx`,
  `frontend/src/api/endpoints.ts`

## PDF upload feature
- `frontend/src/pages/Import.tsx` + `Import.test.tsx` **(new)**
- `frontend/src/api/endpoints.ts` (also touched above)

## MerchantLearningService bug fix
- `backend/.../service/MerchantLearningService.java` + `test/.../MerchantLearningServiceTest.java`

## Deployment readiness / Railway / production hardening
- `backend/src/main/resources/{application.yml,application-dev.yml}` — server.port, compression,
  graceful shutdown, HikariCP tuning, multipart limits, TRUST_PROXY_HEADERS
- `backend/railway.json` **(new)**
- `backend/Dockerfile` — non-root user, container-aware JVM heap
- `backend/.../config/ProductionConfigValidator.java` **(new)** + test
- `backend/.../config/RateLimitFilter.java` — proxy-header IP resolution + ObjectMapper fix + test
- `backend/.../exception/GlobalExceptionHandler.java` — no longer leaks raw exception messages
  in prod + test **(new)**
- `.gitignore` **(new)** — was completely missing from the repo
- `docs/engineering/deployment-guide.md` **(new)**

## Register/login CORS + routing fix (the one this bundle is most likely for)
- `frontend/src/api/client.ts` + `client.test.ts` **(new)** — VITE_API_BASE_URL normalization
- `frontend/.env.example`, `frontend/src/vite-env.d.ts`

## Admin portal type-error fixes (33 errors → 0)
- `admin-portal/src/context/AdminAuthContext.tsx` — exported AdminAuthState
- `admin-portal/src/test/{mockAdminAuth,pagedResponse}.ts` **(new)**
- `admin-portal/src/api/client.ts` + `client.test.ts` — same VITE_API_BASE_URL fix as user frontend
- `admin-portal/.env.example`, `admin-portal/src/vite-env.d.ts`
- `admin-portal/src/pages/{AuditLog,Users}.tsx` — index signatures
- 13 test files: `ProtectedRoute.test.tsx`, `Sidebar.test.tsx`, `useSavedViews.test.ts`,
  `AuditLog.test.tsx`, `Banks.test.tsx`, `FeatureFlags.test.tsx`, `GlobalRules.test.tsx`,
  `LearningEngine.test.tsx`, `Login.test.tsx`, `MerchantIntelligence.test.tsx`,
  `PlatformAnalytics.test.tsx`, `ReconciliationMonitor.test.tsx`, `SystemHealth.test.tsx`,
  `Users.test.tsx`

## Landing page
- `frontend/src/pages/Landing.tsx` — light/dark theme toggle across the whole page

## Verified before packaging
- `tsc -b` clean on both `frontend/` and `admin-portal/`
- File list cross-checked against a fresh diff of your original upload — 63/63 match, nothing
  missing, nothing extra
- Could not run `mvn test` (no Maven in this sandbox) — please run it after applying this
