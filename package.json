{
  "name": "finora",
  "private": true,
  "description": "Repo-wide dev tooling only (commit hygiene, git hooks). Application code lives in backend/, frontend/, admin-portal/, and mobile/, each with its own package.json / pom.xml.",
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.3.0",
    "@commitlint/config-conventional": "^19.2.2",
    "husky": "^9.1.4"
  }
}
