{
  "name": "finora",
  "private": true,
  "description": "Repo-wide dev tooling only (commit hygiene, git hooks). Application code lives in backend/, frontend/, and admin-portal/, each with its own package.json / pom.xml.",
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.3.0",
    "@commitlint/config-conventional": "^19.2.2",
    "husky": "^9.1.4"
  }
}
