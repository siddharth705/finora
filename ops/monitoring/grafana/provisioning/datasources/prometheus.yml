# Provisioned so the stack is usable the moment it starts, and so what is validated locally is the
# same configuration a production Grafana would import.
apiVersion: 1
datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
